<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Personal Blog</title>
  <link rel="stylesheet" href="/styles.css"> <!-- Ruta para CSS -->
</head>
<body>
  <h1>Bienvenido <%= data.user_name %> a tu blog personal</h1>
  
  <a href="/api/createNote?user_id=<%= data.id %>">Crear nueva nota</a>
  <ul>
    <% if (userNotes.length > 0) { %>
      <% userNotes.forEach(note => { %>
        <li>
          <h2><%= note.title %></h2>
          <p><%= note.content %></p>
          <a href="/api/edit/<%= note.id %>">Editar</a>
          <form action="/api/notes/<%= note.id %>?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit">Eliminar</button>
          </form>
        </li>
      <% }) %>
    <% } else { %>
      <li>No hay notas disponibles.</li>
    <% } %>
    <form action="/api/logout" method="post">
      <button type="submit">Cerrar Sesión</button>
  </ul>
</body>
</html>
