<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Nota</title>
  <link rel="stylesheet" href="/styles.css"> <!-- Ruta para CSS -->
</head>
<body>
  <h1>Editar nota</h1>
  <form action="/api/notes/<%= note.id %>?_method=PUT" method="POST">
    <label for="title">Título:</label>
    <input type="text" id="title" name="title" value="<%= note.title %>" required>
    <br>
    <label for="content">Contenido:</label>
    <textarea id="content" name="content" required><%= note.content %></textarea>
    <br>
    <button type="submit"> Editar </button>
    <form action="/api/backToIndex" method="POST">
      <button type="submit">Volver</button>
  </form>
</body>
</html>
